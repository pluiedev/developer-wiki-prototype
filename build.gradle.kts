plugins {
    // Somehow Loom requires the root project to have the `ideaProject` task and... okay then?
    idea
}

group = "org.quiltmc"
version = "1.0-SNAPSHOT"

repositories {
    mavenCentral()
}